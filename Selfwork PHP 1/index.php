<?php

$int = 1;  // dato Integer
$float = 0.5; // dato Float
$string = "Angelo"; // dato Stringa
$boool = true; // dato Boolean

var_dump($int);
var_dump($float);
var_dump($string);
var_dump($boool);

const NUM_INTERO = 1;
const NUM_FLOAT = 0.5;
const STRINGA = "ANGELO";
const BOOLEANA = true;

var_dump(NUM_INTERO);
var_dump(NUM_FLOAT);
var_dump(STRINGA);
var_dump(BOOLEANA);

?>